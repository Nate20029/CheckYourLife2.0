name: Tests

on:
  push:
    branches:
      - develop

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: go to checkyourlife
        run: cd .\checkyourlife2.0
      - name: Jest run
        run: npm test
